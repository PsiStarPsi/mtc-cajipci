Monitor readout software. Needs full driver tree. To build the shared library type:
qmake
make make_lib
qmake 

To build the monitor type:
make

The binary monitor needs the pci driver to be loaded in order to operate.

